# Site8 Bootstrap Prototype
A Prototype Webpack configuration for Bootstrap with TS/JS and CSS file generation

uses Webpack to generate separate CSS and JS files as is in the existing setup for Foundation
incorporates Bootstrap
